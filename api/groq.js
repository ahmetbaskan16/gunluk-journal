export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Yalnızca POST desteklenir'});const {key,content}=req.body||{};const userKey=(typeof key==='string'?key.trim():'')||process.env.GROQ_API_KEY||'';if(!userKey)return res.status(400).json({error:'Groq API anahtarı eksik'});if(!content||typeof content!=='string')return res.status(400).json({error:'İstek içeriği eksik'});const models=['llama-3.3-70b-versatile','gemma2-9b-it','mixtral-8x7b-32768'];let last={};try{for(const model of models){const upstream=await fetch('https://api.groq.com/openai/v1/chat/completions',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${userKey}`},body:JSON.stringify({model,messages:[{role:'system',content:'Türkçe yanıt ver. Empatik, doğal, kısa ve güvenli ol. Tıbbi teşhis koyma.'},{role:'user',content}],temperature:.7})});const data=await upstream.json().catch(()=>({}));if(upstream.ok)return res.status(200).json({text:data?.choices?.[0]?.message?.content||'Groq boş yanıt döndürdü.',model});last=data;if(!String(data?.error?.message||'').toLowerCase().includes('model'))break}const msg=last?.error?.message||'Groq isteği başarısız oldu';return res.status(502).json({error:msg})}catch(error){return res.status(502).json({error:'Groq bağlantısı kurulamadı'})}}