export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Yalnızca POST desteklenir'});const {key,content}=req.body||{};const userKey=typeof key==='string'?key.trim():'';if(!userKey)return res.status(400).json({error:'Groq API anahtarı eksik'});if(!content||typeof content!=='string')return res.status(400).json({error:'İstek içeriği eksik'});try{const upstream=await fetch('https://api.groq.com/openai/v1/chat/completions',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${userKey}`},body:JSON.stringify({model:'llama-3.1-8b-instant',messages:[{role:'system',content:'Türkçe yanıt ver. Empatik, doğal, kısa ve güvenli ol. Tıbbi teşhis koyma.'},{role:'user',content}],temperature:.7})});const data=await upstream.json().catch(()=>({}));if(!upstream.ok){const msg=data?.error?.message||`Groq HTTP ${upstream.status}`;return res.status(upstream.status).json({error:msg})}return res.status(200).json({text:data?.choices?.[0]?.message?.content||'Groq boş yanıt döndürdü.'})}catch(error){return res.status(502).json({error:'Groq bağlantısı kurulamadı'})}}