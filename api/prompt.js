export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({text:'Yalnızca POST'});const prompts=['Bugün kendinle ilgili hangi küçük şeyi fark ettin?','Şu anda hayatında daha çok yer açmak istediğin şey ne?','Son zamanlarda seni gururlandıran bir anı anlat.','Bugün sana iyi gelen üç şeyi yaz.','Bir yıl sonraki kendine ne söylemek isterdin?'];const text=prompts[Math.floor(Math.random()*prompts.length)];if(process.env.OPENAI_API_KEY){try{const r=await fetch('https://api.openai.com/v1/chat/completions',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${process.env.OPENAI_API_KEY}`},body:JSON.stringify({model:process.env.OPENAI_MODEL||'gpt-4o-mini,messages:[{role:'user',content:'Türkçe, düşündürücü tek bir günlük sorusu üret. Sadece soruyu döndür.'} ],temperature:.9})});const j=await r.json();if(j.choices?.[0]?.message?.content)return res.json({text:j.choices[0].message.content})}catch{}}res.json({text})}